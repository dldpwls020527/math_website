# 🌐 외부에서 웹사이트 접속하는 방법 정리

Math 웹사이트를 외부에서 접속할 수 있도록 설정하는 모든 방법을 정리했습니다.

---

## 📌 방법 비교표

| 방법 | 접속 범위 | 난이도 | 속도 | 비용 | URL 영구성 |
|------|----------|--------|------|------|-----------|
| **같은 Wi-Fi** | 로컬 네트워크만 | ⭐ 쉬움 | ⚡⚡⚡ | 무료 | 영구 |
| **ngrok** | 인터넷 전체 | ⭐ 쉬움 | ⚡⚡⚡ | 무료 | 임시 |
| **Railway** | 인터넷 전체 | ⭐⭐ 보통 | ⚡⚡ | 무료 | 영구 |
| **Render** | 인터넷 전체 | ⭐⭐ 보통 | ⚡⚡ | 무료 | 영구 |
| **포트 포워딩** | 인터넷 전체 | ⭐⭐⭐ 어려움 | ⚡⚡⚡ | 무료 | 영구 |

---

## 방법 1: 같은 Wi-Fi 네트워크에서 접속 ⭐ 가장 간단

**용도**: 집/사무실에서 같은 Wi-Fi에 연결된 다른 기기(폰, 태블릿 등)에서 접속

### 단계별 가이드

#### 1단계: 서버 실행
```powershell
python app.py
```
서버가 실행되면 다음과 같은 메시지가 표시됩니다:
```
 * Running on http://0.0.0.0:5000
```

#### 2단계: 컴퓨터의 IP 주소 확인
```powershell
ipconfig
```
출력에서 **IPv4 주소** 찾기 (예: `192.168.1.100`)

#### 3단계: 다른 기기에서 접속
같은 Wi-Fi에 연결된 스마트폰, 태블릿, 다른 컴퓨터의 브라우저에서:
```
http://192.168.1.100:5000
```

**예시**:
- 컴퓨터 IP: `192.168.1.100`
- 접속 주소: `http://192.168.1.100:5000`

### 방화벽 허용 (필요한 경우)

Windows 방화벽이 포트를 차단하는 경우:

```powershell
# PowerShell을 관리자 권한으로 실행 후
New-NetFirewallRule -DisplayName "Flask App" -Direction Inbound -Protocol TCP -LocalPort 5000 -Action Allow
```

또는 Windows 방화벽 설정에서 수동으로 허용:
1. Windows 보안 → 방화벽 및 네트워크 보호
2. 고급 설정
3. 인바운드 규칙 → 새 규칙
4. 포트 → TCP → 특정 로컬 포트: 5000 → 연결 허용

### 장점
- ✅ 즉시 사용 가능
- ✅ 설정 불필요
- ✅ 완전 무료
- ✅ 빠른 속도

### 단점
- ❌ 같은 Wi-Fi 네트워크에서만 접속 가능
- ❌ 외부 인터넷에서는 접속 불가

---

## 방법 2: ngrok 사용 (인터넷 전체 접속) ⭐⭐ 가장 빠른 외부 접속

**용도**: 전 세계 어디서나 인터넷으로 접속 가능

### 단계별 가이드

#### 1단계: ngrok 설치

**Windows (Chocolatey 사용)**:
```powershell
choco install ngrok
```

**Windows (수동 설치)**:
1. https://ngrok.com/download 에서 다운로드
2. 압축 해제 후 `ngrok.exe`를 PATH에 추가하거나 현재 폴더에 저장

#### 2단계: ngrok 계정 생성 및 인증
1. https://ngrok.com 에서 무료 가입
2. https://dashboard.ngrok.com/get-started/your-authtoken 에서 인증 토큰 복사
3. 인증 토큰 설정:
```powershell
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

#### 3단계: Flask 서버 실행
```powershell
# 터미널 1
python app.py
```

#### 4단계: ngrok 터널 시작
```powershell
# 터미널 2 (새 PowerShell 창 열기)
ngrok http 5000
```

#### 5단계: 외부 URL 사용
ngrok이 다음과 같은 URL을 제공합니다:
```
Forwarding    https://abc123-def456.ngrok.io -> http://localhost:5000
```

이 `https://abc123-def456.ngrok.io` URL을 **누구에게나 공유**할 수 있습니다!
- 스마트폰 데이터로 접속 가능
- 다른 국가에서도 접속 가능
- HTTPS 자동 지원

### ngrok 웹 인터페이스
ngrok 실행 중에 http://127.0.0.1:4040 에서 트래픽 모니터링 가능

### 장점
- ✅ 즉시 인터넷 접속 가능
- ✅ HTTPS 자동 지원
- ✅ 설정 간단
- ✅ 무료 플랜 제공

### 단점
- ❌ 무료 플랜은 ngrok을 중지하면 URL 변경
- ❌ 무료 플랜은 세션 제한 (8시간 등)

---

## 방법 3: Railway 배포 (영구 URL) ⭐⭐⭐ 추천

**용도**: 영구적인 외부 접속이 필요한 경우

### 단계별 가이드

#### 1단계: GitHub 저장소 준비
```powershell
# Git 초기화 (아직 안 했다면)
git init
git add .
git commit -m "Initial commit"

# GitHub에 저장소 생성 후
git remote add origin https://github.com/YOUR_USERNAME/math-website.git
git branch -M main
git push -u origin main
```

#### 2단계: Railway 계정 생성
1. https://railway.app 접속
2. "Start a New Project" 클릭
3. GitHub 계정으로 로그인

#### 3단계: 프로젝트 배포
1. "Deploy from GitHub repo" 선택
2. 저장소 선택 (`math-website` 또는 프로젝트 이름)
3. 자동으로 배포 시작

#### 4단계: 도메인 확인
- 배포 완료 후 Railway가 자동으로 도메인 제공
- 예: `https://math-website-production.up.railway.app`
- Settings → Domains에서 커스텀 도메인도 설정 가능

### 장점
- ✅ 영구 URL 제공
- ✅ HTTPS 자동 지원
- ✅ GitHub 연동으로 자동 배포
- ✅ 무료 플랜 제공 (월 500시간)
- ✅ 24/7 운영

### 단점
- ❌ GitHub 저장소 필요
- ❌ 배포 시간 소요 (5-10분)

---

## 방법 4: Render 배포 (영구 URL)

**용도**: Railway 대안, 영구 외부 접속

### 단계별 가이드

#### 1단계: GitHub 저장소 준비
(Railway와 동일)

#### 2단계: Render 계정 생성
1. https://render.com 접속
2. GitHub 계정으로 가입

#### 3단계: 웹 서비스 생성
1. "New +" → "Web Service" 클릭
2. GitHub 저장소 선택
3. 설정:
   - **Name**: `math-website`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`
4. "Create Web Service" 클릭

#### 4단계: 도메인 확인
- 자동으로 도메인 제공 (예: `https://math-website.onrender.com`)

### 장점
- ✅ 영구 URL
- ✅ HTTPS 자동
- ✅ 무료 플랜

### 단점
- ❌ 무료 플랜은 15분 비활성 시 자동 정지 (첫 접속 시 느릴 수 있음)

---

## 방법 5: 포트 포워딩 (고급) ⭐⭐⭐⭐

**용도**: 공인 IP를 가진 경우, 직접 서버 운영

### 단계별 가이드

#### 1단계: 공인 IP 확인
```powershell
# 브라우저에서
https://whatismyipaddress.com/
```

#### 2단계: 공유기 설정
1. 공유기 관리 페이지 접속 (보통 `192.168.1.1` 또는 `192.168.0.1`)
2. 포트 포워딩 설정:
   - 외부 포트: 5000 (또는 원하는 포트)
   - 내부 IP: 컴퓨터 IP (예: `192.168.1.100`)
   - 내부 포트: 5000
   - 프로토콜: TCP

#### 3단계: 방화벽 설정
(방법 1의 방화벽 설정 참고)

#### 4단계: 접속
```
http://공인IP:5000
```

### 주의사항
- ⚠️ 보안 위험: 직접 인터넷에 노출됨
- ⚠️ 공인 IP가 변경될 수 있음 (동적 IP)
- ⚠️ ISP에서 포트 차단 가능

### 장점
- ✅ 완전한 제어
- ✅ 빠른 속도

### 단점
- ❌ 설정 복잡
- ❌ 보안 위험
- ❌ 공인 IP 필요

---

## 🎯 상황별 추천 방법

### 빠른 테스트 (1-2분)
→ **ngrok 사용**

### 같은 집/사무실에서 접속
→ **같은 Wi-Fi 접속 방법**

### 영구적인 공개 웹사이트
→ **Railway 또는 Render 배포**

### 개발/테스트 중
→ **같은 Wi-Fi + ngrok 조합**

---

## 🔍 문제 해결

### "Connection refused" 오류
- `host='0.0.0.0'` 설정 확인 (이미 설정됨)
- 방화벽에서 포트 허용 확인
- 다른 프로그램이 포트를 사용 중인지 확인:
```powershell
netstat -ano | findstr :5000
```

### 포트가 이미 사용 중
```powershell
# 다른 포트 사용
$env:PORT=8080
python app.py
```

### ngrok이 작동하지 않음
- 인증 토큰 설정 확인
- ngrok이 최신 버전인지 확인
- 방화벽/안티바이러스 확인

### Railway/Render 배포 실패
- `requirements.txt`에 모든 패키지 포함 확인
- 로그 확인 (배포 플랫폼에서 제공)
- `Procfile` 또는 시작 명령어 확인

---

## 📱 스마트폰에서 접속 테스트

### 같은 Wi-Fi 방법
1. 컴퓨터와 스마트폰이 같은 Wi-Fi 연결
2. 컴퓨터 IP 주소 확인 (`ipconfig`)
3. 스마트폰 브라우저에서 `http://192.168.1.100:5000` 접속

### ngrok 방법
1. ngrok 실행 후 제공된 URL 복사
2. 스마트폰 데이터(Wi-Fi 끔)로 전환
3. 브라우저에서 ngrok URL 접속

### Railway/Render 방법
1. 배포된 URL 확인
2. 스마트폰 브라우저에서 접속 (Wi-Fi/데이터 모두 가능)

---

## 💡 추가 팁

1. **포트 번호 변경**: 포트 5000이 사용 중이면 8080 사용
   ```powershell
   $env:PORT=8080
   python app.py
   ```

2. **HTTPS 강제**: Railway, Render는 자동으로 HTTPS 제공

3. **도메인 연결**: Railway, Render에서 커스텀 도메인 연결 가능

4. **로그 확인**: 클라우드 배포 시 배포 플랫폼의 로그에서 오류 확인

---

## 🚀 빠른 시작 (추천 순서)

1. **같은 Wi-Fi에서 테스트** (1분)
   ```powershell
   python app.py
   # IP 확인 후 다른 기기에서 접속
   ```

2. **ngrok으로 외부 접속 테스트** (5분)
   ```powershell
   python app.py  # 터미널 1
   ngrok http 5000  # 터미널 2
   ```

3. **Railway로 영구 배포** (15분)
   - GitHub 업로드 → Railway 연결 → 자동 배포

---

이제 원하는 방법으로 외부에서 웹사이트에 접속할 수 있습니다! 🎉

